package $package#[[.dao]]#;

#foreach ($import in $imports)
import $import;
#end
import $package#[[.entity.]]#$entityName;
import java.util.List;
import org.apache.ibatis.annotations.Param;

/**
  * $entityName 数据库操作
  *
  * Generated by Mybatis-Generator on $generatedTime
  */
public interface I${entityName}Dao {
    List<$entityName> queryList($entityName $entityVar);

    $entityName queryOne($entityName $entityVar);

    $entityName queryById(@Param("id") Integer id);

    void add($entityName $entityVar);

    void update($entityName $entityVar);
}